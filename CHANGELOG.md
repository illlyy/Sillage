# Fcode 更新日志

本文件记录面向用户的重要变化。每次发布时，同时更新仓库根目录的
`RELEASE_NOTES.md`；GitHub Actions 会将该文件写入 GitHub Release 和 `update.json`。

## [0.3.2] - 2026-08-03

### 新增

- Claude 后端真正流式输出：思考逐段出现、正文逐 chunk 生成（不再是整段思考或整段正文一次到达）。
- 「流式渲染 Markdown」开关（默认关闭）：开启后生成过程中实时渲染富文本 Markdown（标题、列表、代码块）。
- 工作面板顶部新增 MCP 服务器状态行：每台服务器的在线/离线/检测中状态一目了然。
- 顶栏搜索/命令面板移入「更多」菜单（命令面板、搜索当前对话、工作面板、导出对话四个入口）。

### 改进

- 修复命令输出折叠/展开后背景不折叠、残留空位导致布局突变的问题。
- 实时会话中 MCP 工具调用以「MCP 工具」卡片呈现（与历史记录一致），不再显示为通用「工具调用」。
- 选中 Skills 现在会真正传达给 Claude 后端（消息内附技能引用）。
- 子代理卡片、MCP/Skills 相关 UI 呈现的细节打磨。

## [0.3.1] - 2026-08-01

### 新增

- 思考强度面板像素星空升级：星云渐变夜空、多色流星与拖尾余烬、错峰点亮入场动画。
- 流星头部造型多样化（五角星、爱心、箭头、方块），多条流星交错划过。

### 改进

- 大规模重构：4 个巨型源文件按职责拆分（聊天界面 8507→1434 行、设置页 5362→736 行、
  聊天宿主 3710→1085 行、协议桥 4249→2960 行），全部为纯搬移、行为不变，全量单测通过。
- 协议桥的静态协议/历史工具独立成类，协议改动风险隔离。

## [0.3.0] - 2026-08-01

### 新增

- Ultra 模式玻璃冲击波动画：在思考强度面板中选择 Ultra 时，从按钮处爆出全屏玻璃冲击波，
  折射、色散与辉光边缘向外扩散后归于平静。
- 思考强度面板的像素星空特效：仅当选择或预览 Ultra 档时，面板淡入闪烁的像素星空与流星。
- Ultra 激活时，思维强度按钮呼吸式主色光环与图标着色。
- 代码内 FcodeLauncherShortcuts、FcodeToolNavigation 等导航与交互组件。

### 改进

- 消息流、历史记录与协议事件的并发与稳定性修复。
- 命令输出存储、子代理模型与设置页导航的健壮性提升。

## [0.2.1] - 2026-07-28

### 改进

- 更新下载改为打开默认浏览器，不再为新更新创建 Android 系统下载任务。
- 更新弹窗补充浏览器下载和安装提示。
- 首次启动时取消并清理旧版遗留的系统下载任务。

## [0.2.0] - 2026-07-28

### 新增

- 独立的“关于应用”页面，展示应用名称、作者、版本号和版本代码。
- 关于页面内的一键检查更新入口。
- 可直接打开公开的 GitHub 仓库。

### 改进

- 将版本和更新入口统一收纳到“关于应用”页面。
- 继续使用生产签名及 APK 完整性校验保障应用内更新安全。

## [0.118.4] - 2026-07-25

### 新增

- 应用内 GitHub Release 更新检查、下载和安装流程。
- 设置页手动检查更新入口和正式版低频自动检查。
- 下载完成通知及待安装状态恢复。

### 安全

- APK SHA-256、包名、版本号和签名证书校验。
- 生产签名从 GitHub Actions Secrets 注入，不再使用仓库内的公开开发密钥。

### 构建

- 新增 Tag 驱动的正式发布工作流。
- 自动生成并上传 `update.json`、`sha256sums.txt` 和正式 APK。
- 单独保存每个版本的 R8 `mapping.txt`。
