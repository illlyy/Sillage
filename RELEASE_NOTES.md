## 修复

- 修复 Codex 端开启 Ultra 后报「There's an issue with the selected model」并断连的问题：模型目录现在始终包含配置的默认模型，app-server 不再回退到内置目录而拒绝自定义模型。
- Codex 端模型相关报错现在会翻译为可操作的中文提示（区分「模型不在目录」「该模型不支持当前强度」「上游不识别该模型」）。
- 修复 Claude 端 bun 版二进制在设备沙箱 seccomp 拦截 `epoll_pwait2` 时退出码 159：现在会给出明确说明，并引导安装 Node.js 后改用 npm 版（node 运行可绕开）。
- 修复切换 Codex / Claude 后端时侧边栏历史对话偶尔混入另一后端会话的问题：切换瞬间即清空旧列表，不再等待后端进程启动完成。
- 修复生成子代理或后台任务期间消息可能一直停在「处理中」不结束的问题：子代理真正完成、目标完成后，现在会正确收尾处理状态并释放任务锁。
