## 新增

- 增加应用内检查更新功能，可从 GitHub Releases 获取最新正式版本。
- 增加 APK 下载完成通知和系统安装器跳转。
- 增加设置页“检查更新”入口；正式版每天最多自动检查一次。

## 安全

- 下载完成后校验 SHA-256、应用包名、`versionCode` 和签名证书。
- 正式构建改为使用 GitHub Actions Secrets 中保存的独立生产签名。

## 发布提示

- 这是切换生产签名后的首个版本时，使用旧开发签名安装的版本不能直接覆盖升级。
- 请先备份重要的终端、项目和配置数据，再卸载旧版本并安装正式版本。
