@@ -38,13 +38,19 @@ internal enum class NativeTurnPhase(val active: Boolean) {
+internal const val NATIVE_PROPOSED_PLAN_PREFIX = "PROPOSED_PLAN|"
+
+internal fun encodeNativeProposedPlan(text: String): String = NATIVE_PROPOSED_PLAN_PREFIX + text
+
+internal fun decodeNativeProposedPlan(content: String): String = content.substringAfter('|')
+
-internal data class NativeConversation(val threadId: String, val title: String, val state: String, val projectPath: String, val favorite: Boolean) { val projectName: String get() = projectPath.trimEnd('/').substringAfterLast('/').ifBlank { "\u65e0\u9879\u76ee" } }
+internal data class NativeConversation(val threadId: String, val title: String, val state: String, val projectPath: String, val favorite: Boolean) { val projectName: String get() = projectPath.trimEnd('/').substringAfterLast('/').ifBlank { "\u93c3\u72bb\u300d\u9429? } }
@@ -71,13 +77,13 @@ internal class NativeChatState {
-    var connectionLabel by mutableStateOf("\u6b63\u5728\u542f\u52a8 Codex\u2026")
+    var connectionLabel by mutableStateOf("\u59dd\uff45\u6e6a\u935a\ue21a\u59e9 Codex\u9225?)
-    var conversationTitle by mutableStateOf("\u65b0\u5bf9\u8bdd")
+    var conversationTitle by mutableStateOf("\u93c2\u677f\ue1ee\u7487?)
@@ -88,6 +94,7 @@ internal class NativeChatState {
+    private var activeProposedPlanItemId = ""
@@ -105,6 +112,7 @@ internal class NativeChatState {
+        activeProposedPlanItemId = ""
@@ -130,7 +138,7 @@ internal class NativeChatState {
-        processingLabel = "\u5904\u7406\u4e2d"
+        processingLabel = "\u6fb6\u52ed\u608a\u6d93?
@@ -166,7 +174,10 @@ internal class NativeChatState {
-            .firstOrNull { messages[it].role == NativeChatRole.ASSISTANT }
+            .firstOrNull {
+                messages[it].role == NativeChatRole.ASSISTANT ||
+                    messages[it].content.startsWith(NATIVE_PROPOSED_PLAN_PREFIX)
+            }
@@ -195,6 +206,49 @@ internal class NativeChatState {
+    private fun proposedPlanMessageId(itemId: String): String = "proposed-plan:${itemId.ifBlank { "current" }}"
+
+    private fun upsertProposedPlan(itemId: String, text: String, append: Boolean, streaming: Boolean) {
+        val resolvedItemId = itemId.ifBlank { activeProposedPlanItemId.ifBlank { "current" } }
+        activeProposedPlanItemId = resolvedItemId
+        val messageId = proposedPlanMessageId(resolvedItemId)
+        val existingIndex = messages.indexOfFirst { it.id == messageId && it.content.startsWith(NATIVE_PROPOSED_PLAN_PREFIX) }
+        val previous = if (existingIndex >= 0) decodeNativeProposedPlan(messages[existingIndex].content) else ""
+        val next = if (append) previous + text else text.ifEmpty { previous }
+        val message = NativeChatMessage(
+            id = messageId,
+            role = NativeChatRole.ACTIVITY,
+            content = encodeNativeProposedPlan(next),
+            streaming = streaming,
+            revealStartedAt = if (existingIndex >= 0) messages[existingIndex].revealStartedAt else System.currentTimeMillis(),
+        )
+        if (existingIndex >= 0) messages[existingIndex] = message else messages.add(message)
+        phase = if (streaming) NativeTurnPhase.ANSWERING else phase
+        revision++
+    }
+
+    fun startProposedPlan(raw: String) {
+        val payload = runCatching { JSONObject(raw) }.getOrNull() ?: return
+        val item = payload.optJSONObject("item") ?: payload
+        upsertProposedPlan(item.optString("id", payload.optString("itemId")), item.optString("text"), append = false, streaming = true)
+    }
+
+    fun appendProposedPlanDelta(raw: String) {
+        val payload = runCatching { JSONObject(raw) }.getOrNull() ?: return
+        upsertProposedPlan(payload.optString("itemId", payload.optString("item_id")), payload.optString("delta"), append = true, streaming = true)
+    }
+
+    fun completeProposedPlan(raw: String) {
+        val payload = runCatching { JSONObject(raw) }.getOrNull() ?: return
+        val item = payload.optJSONObject("item") ?: payload
+        upsertProposedPlan(item.optString("id", payload.optString("itemId")), item.optString("text"), append = false, streaming = false)
+    }
+
+    private fun finishProposedPlanIfStreaming() {
+        val index = messages.indexOfLast { it.content.startsWith(NATIVE_PROPOSED_PLAN_PREFIX) && it.streaming }
+        if (index >= 0) messages[index] = messages[index].copy(streaming = false)
+    }
+
@@ -213,12 +267,14 @@ internal class NativeChatState {
-        .replace(Regex("""</?propose_plan\\s*>""", RegexOption.IGNORE_CASE), "")
+        .replace(Regex("""</?propose(?:d)?_plan(?:\\s[^>]*)?>""", RegexOption.IGNORE_CASE), "")
+        // Legacy providers used <propose_plan>; current Codex emits a dedicated
+        // plan item for the official <proposed_plan> block, handled separately above.
@@ -279,8 +335,15 @@ internal class NativeChatState {
+        // onReady restores the per-thread cached plan before history arrives. Keep it as
+        // a fallback because older rollouts may not persist update_plan events.
+        val cachedPlanJson = planJson
+        val cachedPlanExplanation = planExplanation
+        var planPanelIndex = -1
+        planPanelAdded = false
+        activeProposedPlanItemId = ""
@@ -314,11 +377,25 @@ internal class NativeChatState {
+                    planPanelIndex = parsed.size
+        if (planJson == "[]" && cachedPlanJson != "[]") {
+            planJson = cachedPlanJson
+            planExplanation = cachedPlanExplanation
+            planPanelIndex = parsed.size
+        }
+        if (planJson != "[]") {
+            val count = runCatching { JSONArray(planJson).length() }.getOrDefault(0)
+            parsed.add(planPanelIndex.coerceIn(0, parsed.size), NativeChatMessage(
+                role = NativeChatRole.ACTIVITY,
+                content = "PLAN_PANEL|complete|$count",
+            ))
+            planPanelAdded = true
+        }
@@ -328,12 +405,12 @@ internal class NativeChatState {
-            "reasoning" -> "\u6b63\u5728\u601d\u8003"
-            "commandExecution" -> "\u6b63\u5728\u8fd0\u884c\u547d\u4ee4"
-            "fileChange" -> "\u6b63\u5728\u4fee\u6539\u6587\u4ef6"
-            "mcpToolCall" -> "\u6b63\u5728\u8c03\u7528\u5de5\u5177"
-            "webSearch" -> "\u6b63\u5728\u641c\u7d22"
-            else -> "\u5904\u7406\u4e2d"
+            "reasoning" -> "\u59dd\uff45\u6e6a\u93ac\u6fca\u20ac?
+            "commandExecution" -> "\u59dd\uff45\u6e6a\u6769\u612f\ue511\u935b\u6212\u62a4"
+            "fileChange" -> "\u59dd\uff45\u6e6a\u6dc7\ue1bd\u657c\u93c2\u56e6\u6b22"
+            "mcpToolCall" -> "\u59dd\uff45\u6e6a\u748b\u51aa\u6564\u5bb8\u30e5\u53ff"
+            "webSearch" -> "\u59dd\uff45\u6e6a\u93bc\u6ec5\u50a8"
+            else -> "\u6fb6\u52ed\u608a\u6d93?
@@ -383,6 +460,7 @@ internal class NativeChatState {
+        finishProposedPlanIfStreaming()
@@ -394,7 +472,7 @@ internal class NativeChatState {
-        connectionLabel = "\u5df2\u8fde\u63a5"
+        connectionLabel = "\u5bb8\u8336\u7e5b\u93ba?
@@ -402,11 +480,11 @@ internal class NativeChatState {
-                content = message.ifBlank { "Codex \u540e\u7aef\u53d1\u751f\u672a\u77e5\u9519\u8bef" },
+                content = message.ifBlank { "Codex \u935a\u5ea3\ue06c\u9359\u6220\u6553\u93c8\ue046\u7161\u95bf\u6b12\ue1e4" },
-        connectionLabel = "\u8fde\u63a5\u5f02\u5e38"
+        connectionLabel = "\u6769\u70b4\u5e34\u5bee\u509a\u7236"
@@ -434,8 +512,11 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
+    private val pendingPlan = StringBuilder()
+    private var pendingPlanItemId = ""
+    private var planFlushScheduled = false
@@ -446,6 +527,10 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
+    private val flushPlanRunnable = Runnable {
+        planFlushScheduled = false
+        flushPlanDeltas()
+    }
@@ -557,21 +642,38 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        refreshConversations()
-        startBackend()
+        startRuntimeAfterFirstDraw()
+    }
+
+    private fun startRuntimeAfterFirstDraw() {
+        val decor = window.decorView
+        var scheduled = false
+        val listener = object : android.view.ViewTreeObserver.OnDrawListener {
+            override fun onDraw() {
+                if (scheduled) return
+                scheduled = true
+                decor.post {
+                    if (decor.viewTreeObserver.isAlive) decor.viewTreeObserver.removeOnDrawListener(this)
+                    if (isFinishing || isDestroyed) return@post
+                    refreshConversations()
+                    startBackend()
+                }
+            }
+        }
+        decor.viewTreeObserver.addOnDrawListener(listener)
-            chatState.addError("Codex CLI \u5c1a\u672a\u5b89\u88c5\uff0c\u8bf7\u5148\u8fd4\u56de\u9996\u9875\u5b8c\u6210\u8fd0\u884c\u73af\u5883\u5b89\u88c5\u3002")
+            chatState.addError("Codex CLI \u704f\u6c2d\u6e6d\u7039\u590e\ue5ca\u951b\u5c83\ue1ec\u934f\u5823\u7e51\u9365\u70ba\ue6fb\u6924\u975b\u756c\u93b4\u612f\u7e4d\u741b\u5c80\u5e46\u6fa7\u51a8\u7568\u7441\u546b\u20ac?)
-            chatState.addError("\u6ca1\u6709\u53ef\u7528\u7684 API \u914d\u7f6e\uff0c\u8bf7\u5148\u8fd4\u56de\u9996\u9875\u521b\u5efa\u5e76\u542f\u7528\u914d\u7f6e\u3002")
+            chatState.addError("\u5a0c\u2103\u6e41\u9359\ue21c\u6564\u9428?API \u95b0\u5d87\u7586\u951b\u5c83\ue1ec\u934f\u5823\u7e51\u9365\u70ba\ue6fb\u6924\u975b\u57b1\u5be4\u54c4\u82df\u935a\ue21c\u6564\u95b0\u5d87\u7586\u9286?)
@@ -597,7 +699,7 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        chatState.connectionLabel = "\u6b63\u5728\u8fde\u63a5 ${chatState.modelLabel}\u2026"
+        chatState.connectionLabel = "\u59dd\uff45\u6e6a\u6769\u70b4\u5e34 ${chatState.modelLabel}\u9225?
@@ -660,7 +762,7 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-            chatState.modelLabel = profileDefaultModel.ifBlank { "\u9ed8\u8ba4\u6a21\u578b" }
+            chatState.modelLabel = profileDefaultModel.ifBlank { "\u699b\u6a3f\ue17b\u59af\u2033\u7037" }
@@ -743,10 +845,10 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        chatState.messages.add(NativeChatMessage(role = NativeChatRole.ACTIVITY, content = "NOTICE|\u5df2\u4ece\u6b64\u5904\u91cd\u65b0\u751f\u6210"))
+        chatState.messages.add(NativeChatMessage(role = NativeChatRole.ACTIVITY, content = "NOTICE|\u5bb8\u8e6d\u7ca0\u59dd\u3085\ue629\u95b2\u5d86\u67ca\u9422\u71b8\u579a"))
-        chatState.processingLabel = "\u5904\u7406\u4e2d"
+        chatState.processingLabel = "\u6fb6\u52ed\u608a\u6d93?
@@ -768,7 +870,7 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        chatState.processingLabel = "\u5904\u7406\u4e2d"
+        chatState.processingLabel = "\u6fb6\u52ed\u608a\u6d93?
@@ -791,8 +893,8 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        if (chatState.conversationTitle == "\u65b0\u5bf9\u8bdd" && value.isNotBlank()) {
-            chatState.conversationTitle = value.lineSequence().firstOrNull().orEmpty().trim().let { if (it.length > 28) it.take(27) + "\u2026" else it }.ifBlank { "\u65b0\u5bf9\u8bdd" }
+        if (chatState.conversationTitle == "\u93c2\u677f\ue1ee\u7487? && value.isNotBlank()) {
+            chatState.conversationTitle = value.lineSequence().firstOrNull().orEmpty().trim().let { if (it.length > 28) it.take(27) + "\u9225? else it }.ifBlank { "\u93c2\u677f\ue1ee\u7487? }
@@ -881,13 +983,13 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-            Toast.makeText(this, "\u9644\u4ef6\u8bfb\u53d6\u5931\u8d25\uff1a${error.message}", Toast.LENGTH_LONG).show()
+            Toast.makeText(this, "\u95c4\u52ea\u6b22\u7487\u8bf2\u5f47\u6fb6\u8fab\u89e6\u951b?{error.message}", Toast.LENGTH_LONG).show()
-        chatState.connectionLabel = "\u6b63\u5728\u505c\u6b62\u2026"
+        chatState.connectionLabel = "\u59dd\uff45\u6e6a\u934b\u6ec4\ue11b\u9225?
@@ -912,9 +1014,18 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
+            val missingProjectIds = snapshot.asSequence()
+                .map { it.threadId }
+                .filter { !conversationProjectCache.containsKey(it) }
+                .toList()
+            if (missingProjectIds.isNotEmpty()) {
+                CodexAppServerBridge.resolveConversationProjects(missingProjectIds).forEach { (threadId, project) ->
+                    if (project.isNotBlank()) conversationProjectCache[threadId] = project
+                }
+            }
-                val fallbackTitle = title.startsWith("Codex \u4efb\u52a1")
+                val fallbackTitle = title.startsWith("Codex \u6d60\u8bf2\u59df")
@@ -922,10 +1033,8 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-                val project = conversationProjectCache[task.threadId] ?: CodexAppServerBridge
-                    .resolveConversationProject(task.threadId)
-                    .also { resolved -> if (resolved.isNotBlank()) conversationProjectCache[task.threadId] = resolved }
-                if (fallbackTitle && title.startsWith("Codex \u4efb\u52a1")) null
+                val project = conversationProjectCache[task.threadId].orEmpty()
+                if (fallbackTitle && title.startsWith("Codex \u6d60\u8bf2\u59df")) null
@@ -1023,15 +1132,21 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
+        streamHandler.removeCallbacks(flushPlanRunnable)
+        planFlushScheduled = false
+        val droppedPlan = pendingPlan.length
+        pendingPlan.setLength(0)
+        pendingPlanItemId = ""
-            .put("reason", reason).put("droppedReasoning", droppedReasoning).put("droppedAnswer", droppedAnswer))
+            .put("reason", reason).put("droppedReasoning", droppedReasoning)
+            .put("droppedAnswer", droppedAnswer).put("droppedPlan", droppedPlan))
@@ -1043,10 +1158,10 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        chatState.conversationTitle = selectedConversation?.title ?: "\u5bf9\u8bdd"
+        chatState.conversationTitle = selectedConversation?.title ?: "\u7035\u7845\u763d"
-            chatState.processingLabel = "\u6b63\u5728\u91cd\u65b0\u8fde\u63a5\u4efb\u52a1"
+            chatState.processingLabel = "\u59dd\uff45\u6e6a\u95b2\u5d86\u67ca\u6769\u70b4\u5e34\u6d60\u8bf2\u59df"
@@ -1066,9 +1181,9 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        chatState.conversationTitle = "\u65b0\u5bf9\u8bdd"
+        chatState.conversationTitle = "\u93c2\u677f\ue1ee\u7487?
-        chatState.connectionLabel = "\u6b63\u5728\u521b\u5efa\u65b0\u5bf9\u8bdd\u2026"
+        chatState.connectionLabel = "\u59dd\uff45\u6e6a\u9352\u6d98\u7f13\u93c2\u677f\ue1ee\u7487\u6fc3\u20ac?
@@ -1106,8 +1221,6 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        NativeChatDiagnostics.record(this, "reasoning_flush", JSONObject()
-            .put("chars", value.length).put("total", chatState.reasoningText.length))
@@ -1126,8 +1239,18 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-        NativeChatDiagnostics.record(this, "answer_flush", JSONObject()
-            .put("chars", value.length).put("messages", chatState.messages.size))
+    }
+
+    private fun flushPlanDeltas() {
+        streamHandler.removeCallbacks(flushPlanRunnable)
+        planFlushScheduled = false
+        if (pendingPlan.isEmpty()) return
+        val delta = pendingPlan.toString()
+        pendingPlan.setLength(0)
+        chatState.appendProposedPlanDelta(JSONObject()
+            .put("itemId", pendingPlanItemId)
+            .put("delta", delta)
+            .toString())
@@ -1140,7 +1263,7 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
-                chatState.connectionLabel = "\u5df2\u8fde\u63a5"
+                chatState.connectionLabel = "\u5bb8\u8336\u7e5b\u93ba?
@@ -1205,6 +1328,33 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
+            "onPlanStarted" -> {
+                flushAnswerDeltas()
+                flushPlanDeltas()
+                chatState.finishReasoning()
+                chatState.startProposedPlan(value)
+            }
+            "onPlanDelta" -> {
+                flushAnswerDeltas()
+                chatState.finishReasoning()
+                val payload = runCatching { JSONObject(value) }.getOrNull()
+                val itemId = payload?.optString("itemId", payload.optString("item_id")) .orEmpty()
+                val delta = payload?.optString("delta").orEmpty()
+                if (itemId.isNotBlank() && pendingPlanItemId.isNotBlank() && itemId != pendingPlanItemId) flushPlanDeltas()
+                if (itemId.isNotBlank()) pendingPlanItemId = itemId
+                if (delta.isNotEmpty()) pendingPlan.append(delta)
+                if (!planFlushScheduled && pendingPlan.isNotEmpty()) {
+                    planFlushScheduled = true
+                    streamHandler.postDelayed(flushPlanRunnable, 56L)
+                }
+            }
+            "onPlanComplete" -> {
+                flushAnswerDeltas()
+                flushPlanDeltas()
+                chatState.finishReasoning()
+                chatState.completeProposedPlan(value)
+                pendingPlanItemId = ""
+            }
@@ -1256,7 +1406,9 @@ class CodexChatActivity : ComponentActivity(), CodexAppServerBridge.EventListene
+                flushPlanDeltas()
+                pendingPlanItemId = ""